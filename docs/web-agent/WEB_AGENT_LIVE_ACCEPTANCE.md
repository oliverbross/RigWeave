# Web Agent live acceptance

Local macOS debug acceptance passed for an actual `rigweave-stationd` process: TLS 1.3 negotiation, exact certificate pin, signed OBSERVER pairing, authentication, state, Agent-encoded Opus RX, spectrum, waterfall, Hub restart/reconnect, and zero adapter drops during the bounded probe.

Evidence label: `DEMO · NO RADIO`. The tone and deterministic bins are transport evidence only. They are not physical receiver audio, subjective audio, spectrum/RF, platform-vault persistence, cross-device trust, package, or 24-hour soak evidence.
