# M9 mobile sync

M9 adds Direct Station Sync by default and explicit opt-in encrypted cloud transport without changing QSO ownership. Android uses `QsoMutationCoordinator`; Apple uses `QSOStore`. Sync journals are normalized metadata and payload references, not second canonical logbooks.
