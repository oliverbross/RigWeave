# Desktop Functional Gap Ledger v1

Baseline: `de32c8ac908c7979f39bfdfc41ca050378901e75` (`feature/desktop-ui-ux-deep-convergence-v2`).

This ledger records source truth before the functional-closure implementation. UI reachability and deterministic gallery data are not production-owner evidence. Android private data and credentials are not inputs to this audit.

| Foundation | Android authority and storage/protocol | Desktop baseline authority and UI | Missing at baseline | Closure target |
|---|---|---|---|---|
| Home/HamClock | `HamClockHomeScreen`, HamClock registries/providers; owner snapshots and last-good caches | `DesktopParityPlatform.homeModules`; Home/Shack layouts | Modules are registry rows and demo snapshots | Consume typed desktop owners, persist visibility/order/span, expose last-good/empty/error health |
| Neural DX/Outlook | `NeuralOutlookController`, `NeuralDxStore` schema 5; cluster/RBN/PSK/WSPR/weather/solar/QSO evidence | schema-5-equivalent store and `neuralOpportunities` model | No empirical aggregation, calibration, verification, retention, or production model load | Station-scoped 56-day quarter-hour evidence pipeline and 30/60/120-minute empirical outlook |
| Native radio profiles | Android KX, Flex, QMX and reviewed RGO owners; serial/network/UAC protocols | `DesktopRadioController` generic Hamlib/TCI; profile surfaces | Native profile selection, capability/readback, transport and generation-safe lifecycle absent | One radio authority with typed native adapters; disconnected restore; TX acceptance false |
| Native rotator protocols | `AndroidRotatorIntegration`, native drivers/automation/store | `DesktopRotatorController` Hamlib lifecycle and UI protocol list | Native codecs/transports, limits, forbidden sectors, safe path and assignment absent | Typed serial/TCP codecs under the one rotator authority; movement acceptance false |
| Digi engines | `DigiController`, `DigiFtEngine`, native Rust modem C ABI; Digi schema 2 | mode registry and schema-2-equivalent store | No desktop modem/audio session, decode model, slot lifecycle, or SSTV gallery owner | Link existing Rust modem engine, exact-route RX lifecycle, slot/manual workflows, private SSTV store; TX locked |
| DX Chaser | `DxChaserEngine`, scorer/store/ports schema 1 | review string and engagement schema | No eligibility reducer, scoring, target lock, cooldown or journal runtime | Local-decode-only Assist/Dry Run/Chase controller using the Digi owner; cannot enable TX |
| CW/Voice Keyer | Android `KeyerController`, runtime/profile store; one TX/audio authority | macro registry and disabled buttons | No typed profiles, bounded queue, tokens, preview, voice plan, repeat or stop runtime | One keyer owner; preview works; send remains acceptance-gated |
| Intelligence/Awards/Map | `LogIntelligenceRepository`, progress/award models and map owners | QSO/RF summary surface | Award engines, bounded drill-through and complete workspace filters absent | Local-estimate award calculators, paged analytics and shared RF observation map |
| Contest/N1MM | Contest session/store schema 2, scoring/export engines, N1MM typed runtime | staging tables, definition registry and merge review | No session controller, scoring/rate/serial/ESM/export or N1MM codec/lifecycle | Complete staging authority; idempotent merge via canonical QSO coordinator; N1MM disabled/loopback/untrusted |
| Intelligent Band Maps | `BandMapController`/domain over the shared spot repository | four accepted layouts over shared spots | Android-equivalent canonicalisation, ranking, Needs/worked/contest/chaser/outlook evaluation absent | One evaluator/controller consuming shared spots and existing owners; no provider connection |
| DX workspace | Android DX multi-tab workspace and shared providers | routed page using shared spots and Neural foundation | History/search/watchlist/news/RBN/PSK/WSPR/outlook/drill-through incomplete | Complete tabs as projections of existing owners; no direct CAT |
| Portable | `PortableController` and provider/cache/catalogue owners | activity model and logger review UI | Production provider adapters, catalogue search, activation context and handoff absent | POTA/SOTA/WWFF/IOTA adapters with truthful provider states and cached catalogue queries |
| Operations planner | `OperationsController`, calendar providers and cached catalogues | planner labels and routed page | No calendar/catalogue query owner, spatial scope, stable map state or cross-border results | Typed planner over last-good caches; never fetch per pan |
| Satellite/QO-100 | `SatelliteOperationsController`, providers and shared SGP4 C ABI | local fixture passes and RX review | No catalogue/TLE import, observer/favourites/pass controller, Doppler or ICS export | Shared native SGP4 controller, provider cache and receive-only handoffs; no auto follow/TX |
| Groups.io | Groups.io feature/client/store schema 2, outbox and reconciliation | schema-2-equivalent archive/draft tables and review UI | No desktop API client, archive loader, attachment/delivery reconciliation or settings authority | Vault-bound client, offline archive/FTS, drafts/outbox/moderation/ambiguous delivery and fake-service suite |
| Presets/alerts/notifications | Android preset rules and notification policy owners | static preset cards and provider/settings rows | CRUD/persistence/safe recall and native notification abstraction absent | One preset authority and one notification authority; explicit recall; quiet profiles and platform fallback |
| EQ Studio | `EqStudioController`; Elecraft readback/draft/verify/rollback | accepted EQ layout with disabled apply/record | No native Elecraft transaction owner, draft/readback conflict or profile persistence | Eight-band native Elecraft workflow with exclusive CAT transaction; apply gated by proven capability |

Initial totals: 14 `SOURCE_COMPLETE`, 17 `FOUNDATION_WIRED`, 0 `MISSING`, 31 audited rows.

