# Install Linux RC1

Use the x86_64 portable archive or Debian package. The full GUI requires its bundled Qt runtime and a desktop Secret Service provider for credential persistence; credential operations fail closed when unavailable. The stationd archives contain no identity, certificate, private key, credential, database, or recording. No service is installed or started automatically.

