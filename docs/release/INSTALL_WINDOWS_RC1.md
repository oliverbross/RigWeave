# Install Windows RC1

Use the portable ZIP or NSIS setup executable. Both are unsigned RC artifacts, so Windows SmartScreen may warn. Verify the published SHA-256 digest before running. Credentials use Windows Credential Manager and are not included in portable data or support bundles.

