# Install macOS RC1

Unzip the arm64 application and verify its published SHA-256 digest. This RC is unsigned and not notarised, so Gatekeeper may block first launch; no security bypass is recommended. Credentials use macOS Keychain. Signing/notarisation remain a later release boundary.

