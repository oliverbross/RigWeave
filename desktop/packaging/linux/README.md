# Linux package lifecycle

CPack emits TGZ and DEB candidates. The user unit is installed but disabled; enable it only with explicit operator consent. Secrets use libsecret/Secret Service. When unavailable, secrets may remain in memory for the current session but are never written as plaintext. Add the operator only to the narrow serial/audio groups actually required; do not use wildcard udev access.
